#[include]
	# note, this only works with git 1.7.10+
        # for username and tokens
#	path = /Users/theoretick/.gitconfig.local
[user]
  name = theoretick
  user = theoretick
  email = mail@limitlesschannels.com
[color]
	ui = auto
[color "branch"]
	current = yellow
	remote = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
[color "status"]
	added = yellow
	changed = green
	untracked = cyan
